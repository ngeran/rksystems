# Service fronting the astro-app Deployment.
---
apiVersion: v1
kind: Service
metadata:
  name: astro-app
  namespace: default
spec:
  selector:
    app: astro-app
  ports:
    - name: http
      port: 80
      targetPort: http      # named → the container's `http` port (8080, non-root)
